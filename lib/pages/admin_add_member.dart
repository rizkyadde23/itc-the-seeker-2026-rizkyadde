import 'package:flutter/material.dart';

class AddMemberPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tambah Anggota")),
      body: Center(child: Text("Form tambah anggota")),
    );
  }
}